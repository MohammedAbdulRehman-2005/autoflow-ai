fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
python-jose[cryptography]>=3.3.0
python-dotenv==1.01
bcrypt>=4.1.0
redis>=5.0.0
pydantic[email]>=2.7.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9
groq>=0.9.0
openai==1.41
httpx>=0.27.0
cryptography>=42.0.0
jinja2>=3.1.0
# resend email API is called via httpx (no extra package needed)
apscheduler>=3.10.4
google-api-python-client
google-auth
google-auth-oauthlib
google-auth-httplib2

# ── Celery + production task queue ──────────────────────────────────────────
celery[redis]>=5.3.0          # Celery with Redis support
flower>=2.0.0                 # Celery monitoring dashboard (port 5555)
celery-redbeat>=2.0.0         # Redis-backed Beat scheduler (survives restarts)

# ── LangGraph AI Agents ───────────────────────────────────────────────────
langchain-core>=0.2.10
langchain-groq>=0.1.6
langgraph>=0.1.14
# ── Production Readiness ──────────────────────────────────────────────────
sentry-sdk>=2.5.1             # Error tracking
slowapi>=0.1.9                # Rate limiting for FastAPI
